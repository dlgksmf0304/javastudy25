package ch05.packagetest.package2;

public class B {
	// 접근제한자 연습
//	A a;
	C c;
	
	//A는 public이 아니라 default이기 때문에 다른 패키지인 B에서 A를 불러 올 수 없다.(오류떠서 주석처리함)
	//C는 같은 패키지기 때문에 불러 올 수 있고 import는 같은패키지라 뜨지않는다.
	//import는 다른 패키지에서 불러왔을 경우에만 써진다.
	
}
