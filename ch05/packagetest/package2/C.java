package ch05.packagetest.package2;

public class C {
	// 접근제한자 연습
//	A a;
	B b;
	
	//A는 public이 아니라 default이기 때문에 다른 패키지인 C에서 A를 불러 올 수 없다.(오류떠서 주석처리함)
	//B와 마찬가지로 B,C는 같은 패키지라 import가 뜨지 않고 다른 패키지에서 불러왔을때만 뜬다(A참고)
	
}
